import { LucideProps, Chrome, Github, FileText } from "lucide-react";

export const GoogleDriveIcon = (props: LucideProps) => (
  <Chrome {...props} />
);

export const NotionIcon = (props: LucideProps) => (
  <FileText {...props} />
);

export const GitHubIcon = (props: LucideProps) => (
  <Github {...props} />
);
